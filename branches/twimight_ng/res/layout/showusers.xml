<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_height="fill_parent" android:layout_width="fill_parent"
	android:orientation="vertical">

	<LinearLayout android:id="@+id/headerBar"
		android:layout_height="50dp" android:layout_width="match_parent"
		android:orientation="horizontal" android:background="@drawable/top_bar_background"
		android:gravity="center_vertical">
		<Button android:id="@+id/headerBarFriendsButton"
			android:layout_width="wrap_content" android:layout_height="fill_parent"
			android:text="Friends" android:layout_marginRight="10dp"
			android:textSize="12sp" android:textStyle="bold" android:paddingLeft="2dp"
			android:paddingRight="2dp" />
		<Button android:id="@+id/headerBarFollowersButton"
			android:layout_width="wrap_content" android:layout_height="fill_parent"
			android:text="Followers" android:layout_marginRight="10dp"
			android:textSize="12sp" android:textStyle="bold" android:paddingLeft="2dp"
			android:paddingRight="2dp" />
	</LinearLayout>
	<ListView android:layout_height="fill_parent"
		android:layout_width="fill_parent" android:id="@+id/userList"
		android:fastScrollEnabled="true" android:scrollingCache="true" />

</LinearLayout>
